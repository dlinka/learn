<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cr.dao.UserMapper">

    <select id="selectUserById" resultType="User">
        select * from user where user_id = #{userId}
    </select>

    <select id="selectUserByCompanyId" resultType="User">
        select * from user where company_id = #{companyId}
    </select>

    <select id="selectMapUser" resultType="map">
        select * from user where user_id = #{userId}
    </select>

    <select id="selectUsers" resultType="User">
        select * from user
    </select>

    <select id="selectMapUsers" resultType="User">
        select * from user
    </select>

    <insert id="addUser" useGeneratedKeys="true" keyProperty="userId">
        insert into user(name, nick_name) values(#{name}, #{nickName});
    </insert>

    <update id="updateUser">
        update user set name = #{name}, nick_name = #{nickName} where user_id = #{userId}
    </update>

    <update id="updateUserNickName">
        update user set nick_name = #{nickName} where user_id = #{userId}
    </update>

    <delete id="deleteUser">
        delete from user where user_id = #{userId}
    </delete>

</mapper>