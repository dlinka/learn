<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cr.dao.UserMapper">

    <select id="selectUser" resultType="User">
        select * from user where id = #{uid}
    </select>

    <insert id="addUser" useGeneratedKeys="true" keyProperty="uid">
        insert into user(name, nick_name) values(#{name}, #{nickName});
    </insert>

    <update id="updateUser">
        update user set name = #{name}, nick_name = #{nickName} where id = #{uid}
    </update>

    <update id="updateUserNickName">
        update user set nick_name = #{nickName} where id = #{uid}
    </update>

    <delete id="deleteUser">
        delete from user where id = #{uid}
    </delete>

</mapper>